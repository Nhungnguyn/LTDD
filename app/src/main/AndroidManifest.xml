<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DrivingTestApp"
        tools:targetApi="31"
        android:networkSecurityConfig="@xml/network_security_config"> <!-- Thêm tham chiếu tới network_security_config.xml -->

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TopWrongActivity" />
        <activity android:name=".CriticalQuestionsActivity" />
        <activity android:name=".MemoryAidActivity" />
        <activity android:name=".TrafficSignsActivity" />
        <activity android:name=".ReviewQuestionsActivity" />
        <activity android:name=".ViewIncorrectActivity" />
        <activity android:name=".TestBySetActivity" />
        <activity android:name=".RandomTestActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".TestResultActivity" />
        <activity android:name=".ForgotPasswordActivity" />

    </application>

    <!-- Quyền Internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
</manifest>
